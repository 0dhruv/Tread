# System Architecture

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT BROWSER                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Modern SPA (Static/index.html)              │  │
│  │  • Vanilla JavaScript  • Plotly Charts  • Responsive UI  │  │
│  └────────────────────┬─────────────────────────────────────┘  │
└────────────────────────┼────────────────────────────────────────┘
                         │ HTTP/REST API
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     FASTAPI APPLICATION                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    API Layer (Routes)                     │  │
│  │  ┌──────────────┬──────────────┬──────────────┬─────────┐│  │
│  │  │ Auth Routes  │ Stock Routes │Trading Routes│AI Routes││  │
│  │  │ /api/auth/*  │ /api/stocks/*│/api/trading/*│/api/ai/*││  │
│  │  └──────────────┴──────────────┴──────────────┴─────────┘│  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │                                       │
│  ┌──────────────────────┴───────────────────────────────────┐  │
│  │              Business Logic (Services)                    │  │
│  │  ┌──────────────────┬──────────────────┬────────────────┐│  │
│  │  │Market Collector  │ Paper Trading    │  AI Assistant  ││  │
│  │  │  (yfinance)      │   (Buy/Sell)     │   (Gemini)     ││  │
│  │  └──────────────────┴──────────────────┴────────────────┘│  │
│  └──────────────────────┬───────────────────────────────────┘  │
│                         │                                       │
│  ┌──────────────────────┴───────────────────────────────────┐  │
│  │              Data Layer (SQLAlchemy ORM)                  │  │
│  │  ┌──────┬──────────┬──────┬──────────┬──────────────────┐│  │
│  │  │Stock │MarketData│ User │Portfolio │  Transaction     ││  │
│  │  │Model │  Model   │Model │  Model   │     Model        ││  │
│  │  └──────┴──────────┴──────┴──────────┴──────────────────┘│  │
│  └──────────────────────┬───────────────────────────────────┘  │
└────────────────────────┼────────────────────────────────────────┘
                         │ SQL Queries
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    POSTGRESQL DATABASE                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                        Tables                             │  │
│  │  • stocks (7500+ rows)     • portfolios                   │  │
│  │  • market_data (millions)  • transactions                 │  │
│  │  • users                                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                            │
│  ┌──────────────────┐        ┌──────────────────────────────┐  │
│  │   yfinance API   │        │   Google Gemini API          │  │
│  │  (Yahoo Finance) │        │   (Free Tier: 60 req/min)    │  │
│  │  • Stock prices  │        │   • AI Analysis              │  │
│  │  • Market cap    │        │   • Chat interface           │  │
│  │  • Historical    │        │   • Portfolio insights       │  │
│  └──────────────────┘        └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow

### 1. User Authentication Flow
```
User Login Request
    ↓
FastAPI Auth Route (/api/auth/login)
    ↓
Verify credentials (bcrypt)
    ↓
Generate JWT token
    ↓
Return token to client
    ↓
Client stores token (localStorage)
    ↓
Future requests include token in Authorization header
```

### 2. Stock Data Collection Flow
```
Script/Admin Trigger
    ↓
MarketDataCollector Service
    ↓
ThreadPoolExecutor (concurrent downloads)
    ↓
yfinance API calls (multiple stocks in parallel)
    ↓
Parse & validate data
    ↓
SQLAlchemy ORM
    ↓
Batch insert/update to PostgreSQL
    ↓
Update indexes
```

### 3. Trading Flow
```
User clicks "Buy" button
    ↓
Frontend validates input
    ↓
POST /api/trading/buy with JWT token
    ↓
Verify authentication (JWT middleware)
    ↓
PaperTradingService.execute_buy()
    │
    ├─ Check user balance
    ├─ Verify stock exists
    ├─ Calculate fees
    ├─ Update user balance
    ├─ Create/update portfolio position
    └─ Record transaction
    ↓
Commit to database
    ↓
Return success response
    ↓
Frontend refreshes portfolio
```

### 4. AI Analysis Flow
```
User requests AI analysis
    ↓
POST /api/ai/analyze-stock
    ↓
Fetch stock data from database
    ↓
Format data for AI context
    ↓
AIAssistant.get_stock_analysis()
    │
    ├─ Combine system prompt + stock data + user query
    ├─ Call Google Gemini API
    ├─ Parse response
    └─ Add ethical disclaimers
    ↓
Return formatted analysis
    ↓
Frontend displays with formatting
```

## 🗄️ Database Schema

```sql
┌─────────────────┐
│     stocks      │
├─────────────────┤
│ id (PK)         │
│ symbol (UK)     │◄──┐
│ name            │   │
│ exchange        │   │
│ sector          │   │
│ market_cap      │   │
│ current_price   │   │
└─────────────────┘   │
                      │
┌─────────────────┐   │FK
│  market_data    │   │
├─────────────────┤   │
│ id (PK)         │   │
│ stock_id (FK)   ├───┘
│ date (UK)       │
│ open_price      │
│ close_price     │
│ volume          │
│ change_percent  │
└─────────────────┘

┌─────────────────┐
│      users      │
├─────────────────┤
│ id (PK)         │◄──┐
│ email (UK)      │   │
│ username (UK)   │   │
│ hashed_password │   │
│ paper_balance   │   │
└─────────────────┘   │
                      │
┌─────────────────┐   │FK
│   portfolios    │   │
├─────────────────┤   │
│ id (PK)         │   │
│ user_id (FK)    ├───┤
│ stock_id (FK)   │   │
│ quantity        │   │
│ avg_buy_price   │   │
│ unrealized_pnl  │   │
└─────────────────┘   │
                      │
┌─────────────────┐   │
│  transactions   │   │
├─────────────────┤   │
│ id (PK)         │   │
│ user_id (FK)    ├───┘
│ stock_id (FK)   │
│ type (BUY/SELL) │
│ quantity        │
│ price           │
│ realized_pnl    │
│ created_at      │
└─────────────────┘
```

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────┐
│           Security Layers                    │
├──────────────────────────────────────────────┤
│                                              │
│  1. Input Validation (Pydantic)              │
│     • Type checking                          │
│     • Range validation                       │
│     • Required fields                        │
│                                              │
│  2. Authentication (JWT)                     │
│     • Token-based auth                       │
│     • Expiration (30 min)                    │
│     • Secure secret key                      │
│                                              │
│  3. Password Security (bcrypt)               │
│     • Hashing with salt                      │
│     • Cost factor: 12                        │
│     • Never store plain text                 │
│                                              │
│  4. SQL Injection Prevention (ORM)           │
│     • Parameterized queries                  │
│     • SQLAlchemy escaping                    │
│     • No raw SQL                             │
│                                              │
│  5. CORS (Whitelist)                         │
│     • Allowed origins only                   │
│     • Credentials support                    │
│     • Method restrictions                    │
│                                              │
└──────────────────────────────────────────────┘
```

## 📦 Component Dependencies

```
┌───────────────┐
│  Main.py      │  FastAPI App Entry Point
└───┬───────────┘
    │
    ├─► Config      (Environment & Settings)
    │
    ├─► Database    (SQLAlchemy + Connection Pool)
    │   │
    │   └─► Models (Stock, MarketData, User, etc.)
    │
    ├─► Auth        (JWT + Password Hashing)
    │
    ├─► API Routes
    │   │
    │   ├─► auth_routes.py
    │   ├─► stock_routes.py
    │   ├─► trading_routes.py
    │   └─► ai_routes.py
    │
    └─► Services
        │
        ├─► market_data_collector.py
        │   └─► yfinance
        │
        ├─► paper_trading.py
        │   └─► Transaction Logic
        │
        └─► ai_assistant.py
            └─► Google Gemini API
```

## 🎨 Frontend Architecture

```
┌─────────────────────────────────────┐
│         index.html (SPA)            │
├─────────────────────────────────────┤
│                                     │
│  ├─ Navigation Bar                  │
│  │                                  │
│  ├─ Dashboard Page                  │
│  │  ├─ Portfolio Stats              │
│  │  ├─ Market Movers                │
│  │  └─ AI Insights                  │
│  │                                  │
│  ├─ Markets Page                    │
│  │  ├─ Search & Filters             │
│  │  └─ Stock Table                  │
│  │                                  │
│  ├─ Portfolio Page                  │
│  │  ├─ Holdings Table               │
│  │  └─ Transaction History          │
│  │                                  │
│  └─ AI Assistant Page               │
│     └─ Chat Interface               │
│                                     │
└──────────┬──────────────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌─────────┐  ┌─────────┐
│style.css│  │ app.js  │
│         │  │         │
│ Modern  │  │ • API   │
│ Dark UI │  │ • State │
│ Gradients│ │ • Events│
│ Glass   │  │         │
└─────────┘  └─────────┘
```

## 🚀 Deployment Architecture

```
                 ┌─────────────────┐
                 │   Load Balancer │
                 │    (Optional)   │
                 └────────┬────────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ▼               ▼               ▼
    ┌─────────┐     ┌─────────┐     ┌─────────┐
    │ Worker 1│     │ Worker 2│     │ Worker 3│
    │ Uvicorn │     │ Uvicorn │     │ Uvicorn │
    │  :8000  │     │  :8001  │     │  :8002  │
    └────┬────┘     └────┬────┘     └────┬────┘
         │               │               │
         └───────────────┼───────────────┘
                         │
                ┌────────┴────────┐
                │                 │
                ▼                 ▼
         ┌──────────────┐  ┌──────────────┐
         │  PostgreSQL  │  │    Redis     │
         │   Database   │  │  (Optional)  │
         │              │  │    Cache     │
         └──────────────┘  └──────────────┘
```

---

**This architecture ensures scalability, security, and maintainability.**
